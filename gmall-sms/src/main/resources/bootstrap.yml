spring:
  application:
    name: sms-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848 # nacos端口号
        namespace: 681b98ee-0ee3-4040-8d0e-0fac6d0f16e5 # 命名空间的UUID
        group: dev # 分组
        file-extension: yml # 文件后缀


